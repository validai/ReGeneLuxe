/**
 * Legacy Vite entry — not used by the product runtime.
 * Canonical app: Next.js (`npm run dev` → http://127.0.0.1:5174/).
 * Kept only so accidental imports fail loudly in documentation; do not revive.
 */
throw new Error("Vite product entry is retired. Use Next.js: npm run dev → http://127.0.0.1:5174/");
